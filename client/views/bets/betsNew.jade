template(name="betsNew")

  +contentFor "headerButtonLeft"
    +ionNavBackButton(path="bets.pending")

  +contentFor "headerTitle"
    h1.title Nouveau pari

  +ionView
    +ionContent(class="padding")
      +autoForm(collection="Bets" id="createBetForm" type="insert" )

        +afFormGroup name='subject' label-type='stacked'
        +afFormGroup name='profit' label-type='stacked'
        +afFormGroup name='yes_player_name' label-type='stacked'
        +afFormGroup name='no_player_name' label-type='stacked'

        button(type="submit" class="btn btn-primary") Zou, un item de + !